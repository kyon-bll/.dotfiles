;; カーソル位置を記憶
(save-place-mode 1)

;; ミニバッファ履歴をemacs終了時も保存する
(savehist-mode 1)

;; ログの記録行数を増やす
(setq message-log-max 10000)

;; 履歴をたくさん保存する
(setq history-length 1000)

;; バッファ、ウィンドウ保存
(desktop-save-mode 1)
